export function FinalTakeaway() {
  return (
    <section id="takeaway" className="scroll-mt-6">
      <h2 className="text-lg font-semibold text-[var(--foreground)] mb-3">Final takeaway</h2>
      <p className="text-[var(--card-foreground)] leading-relaxed max-w-2xl rounded-lg border border-[var(--border)] bg-[var(--card)] p-5">
        YO is not simply a yield UI. It functions as a structured yield routing system built on ERC-4626 vaults,
        operator-managed strategy allocation and layered protocol risk.
      </p>
    </section>
  );
}
